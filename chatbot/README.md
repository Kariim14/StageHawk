# Chatbot Placeholder

Reserved for a future chatbot adapter.

The chatbot should summarize `reports/final_report.json` and query `case_manifest.json`; it should not run tool-specific scripts directly.
